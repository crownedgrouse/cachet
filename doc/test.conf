[
 % write 1->20 by increment of 1, delete 1->10 by increment of 1
 {set,    cache, id,          write,  write_ram, {,}}
,{set,    cache, timestamp,   write,  write_ram, {,}}
,{set,    cache, term,        write,  write_ram, {,}}
,{set,    cache, id,          delete, delete_ram, {,}}
,{set,    cache, timestamp,   delete, delete_ram, {,}}
,{set,    cache, term,        delete, delete_ram, {,}}
,{set,    cache, id,          write,  write_disc, {,}}
,{set,    cache, timestamp,   write,  write_disc, {,}}
,{set,    cache, term,        write,  write_disc, {,}}
,{set,    cache, id,          delete, delete_disc, {,}}
,{set,    cache, timestamp,   delete, delete_disc, {,}}
,{set,    cache, term,        delete, delete_disc, {,}}

,{set,    split, id,          write,  write_ram, {,}}
,{set,    split, timestamp,   write,  write_ram, {,}}
,{set,    split, term,        write,  write_ram, {,}}
,{set,    split, id,          delete, delete_ram, {,}}
,{set,    split, timestamp,   delete, delete_ram, {,}}
,{set,    split, term,        delete, delete_ram, {,}}
,{set,    split, id,          write,  write_disc, {,}}
,{set,    split, timestamp,   write,  write_disc, {,}}
,{set,    split, term,        write,  write_disc, {,}}
,{set,    split, id,          delete, delete_disc, {,}}
,{set,    split, timestamp,   delete, delete_disc, {,}}
,{set,    split, term,        delete, delete_disc, {,}}
 % write 2 x 1->10 (same key, different value), delete 1 -> 10
,{bag,    cache, id,          write,  write_ram, {,}}
,{bag,    cache, timestamp,   write,  write_ram, {,}}
,{bag,    cache, term,        write,  write_ram, {,}}
,{bag,    cache, id,          delete, delete_ram, {,}}
,{bag,    cache, timestamp,   delete, delete_ram, {,}}
,{bag,    cache, term,        delete, delete_ram, {,}}
,{bag,    cache, id,          write,  write_disc, {,}}
,{bag,    cache, timestamp,   write,  write_disc, {,}}
,{bag,    cache, term,        write,  write_disc, {,}}
,{bag,    cache, id,          delete, delete_disc, {,}}
,{bag,    cache, timestamp,   delete, delete_disc, {,}}
,{bag,    cache, term,        delete, delete_disc, {,}}

,{bag,    split, id,          write,  write_ram, {,}}
,{bag,    split, timestamp,   write,  write_ram, {,}}
,{bag,    split, term,        write,  write_ram, {,}}
,{bag,    split, id,          delete, delete_ram, {,}}
,{bag,    split, timestamp,   delete, delete_ram, {,}}
,{bag,    split, term,        delete, delete_ram, {,}}
,{bag,    split, id,          write,  write_disc, {,}}
,{bag,    split, timestamp,   write,  write_disc, {,}}
,{bag,    split, term,        write,  write_disc, {,}}
,{bag,    split, id,          delete, delete_disc, {,}}
,{bag,    split, timestamp,   delete, delete_disc, {,}}
,{bag,    split, term,        delete, delete_disc, {,}}
 % write shuffled 1->20 by increment of 1, delete 1->10 shuffled by increment of 1
,{ord_set,cache, id,          write,  write_ram, {,}}
,{ord_set,cache, timestamp,   write,  write_ram, {,}}
,{ord_set,cache, term,        write,  write_ram, {,}}
,{ord_set,cache, id,          delete, delete_ram, {,}}
,{ord_set,cache, timestamp,   delete, delete_ram, {,}}
,{ord_set,cache, term,        delete, delete_ram, {,}}
,{ord_set,cache, id,          write,  write_disc, {,}}
,{ord_set,cache, timestamp,   write,  write_disc, {,}}
,{ord_set,cache, term,        write,  write_disc, {,}}
,{ord_set,cache, id,          delete, delete_disc, {,}}
,{ord_set,cache, timestamp,   delete, delete_disc, {,}}
,{ord_set,cache, term,        delete, delete_disc, {,}}

,{ord_set,split, id,          write,  write_ram, {,}}
,{ord_set,split, timestamp,   write,  write_ram, {,}}
,{ord_set,split, term,        write,  write_ram, {,}}
,{ord_set,split, id,          delete, delete_ram, {,}}
,{ord_set,split, timestamp,   delete, delete_ram, {,}}
,{ord_set,split, term,        delete, delete_ram, {,}}
,{ord_set,split, id,          write,  write_disc, {,}}
,{ord_set,split, timestamp,   write,  write_disc, {,}}
,{ord_set,split, term,        write,  write_disc, {,}}
,{ord_set,split, id,          delete, delete_disc, {,}}
,{ord_set,split, timestamp,   delete, delete_disc, {,}}
,{ord_set,split, term,        delete, delete_disc, {,}}].




